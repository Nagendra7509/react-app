import styled from "@emotion/styled";
import tw from "tailwind.macro";




const PracticeAdvancedConcepts = styled.div `
    ${tw}`;

const Hocs = styled.div `
    ${tw `flex flex-col justify-center items-center`}`;
const RenderProps = styled.div `
    ${tw `flex flex-col justify-center items-center`}`;

const TitleHoc = styled.h1 `
    ${tw `text-3xl p-4`}`;

const TitleRenderProps = styled.h1 `
    ${tw `text-3xl p-4`}`;

export {
    PracticeAdvancedConcepts,
    Hocs,
    RenderProps,
    TitleHoc,
    TitleRenderProps
};
